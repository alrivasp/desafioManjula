<%= form_with(model: baby, local: true) do |form| %>
  <% if baby.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(baby.errors.count, "error") %> prohibited this baby from being saved:</h2>

      <ul>
      <% baby.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="text-center">
    <div class="field">
    <%= form.label :name, class:" text-white", style:"font-size:15px; text-shadow: 0.1em 0.1em 0.05em #333" %>
    <%= form.text_field :name %>
    </div>

    <div class="field">
      <%= form.label :photo, class:" text-white", style:"font-size:15px; text-shadow: 0.1em 0.1em 0.05em #333" %>
      <%= form.text_area :photo %>
    </div>

    <div class="actions mt-4">
      <%= form.submit class:"btn btn-primary rounded-pill border border-white"%>
    </div>
  </div>
  
<% end %>
